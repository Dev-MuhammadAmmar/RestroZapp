# RestroZapp

Owner administration panel for RestroZapp POS.

## Development

1. Copy `.env.example` to `.env.local`.
2. Add the Supabase publishable and service-role keys.
3. Install dependencies with `npm install`.
4. Start the dashboard with `npm run dev`.

The development server runs at `http://localhost:3001`.
